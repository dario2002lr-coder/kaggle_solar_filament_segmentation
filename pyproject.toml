[project]
name = "kaggle-solar-filament-segmentation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kagglehub>=1.0.2",
]

[dependency-groups]
dev = [
    "kaggle>=2.2.4",
]
